// Script para interacciones en el dashboard del super admin.
// Usamos jQuery, que viene con WP.

jQuery(document).ready(function($) {
    // Ejemplo: Al hacer clic en un botón, mostramos una alerta.
    $('button').on('click', function() {
        alert('Botón clicado en el dashboard!');
    });
});